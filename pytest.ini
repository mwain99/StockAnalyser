[pytest]
testpaths = tests
addopts =
    --tb=short
    -v
    --strict-markers
markers =
    unit: fast, isolated unit tests
    integration: slower end-to-end tests requiring a Spark session
python_files = test_*.py
python_classes = Test*
python_functions = test_*
